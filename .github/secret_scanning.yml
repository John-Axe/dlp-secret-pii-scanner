paths-ignore:
  # Benchmark fixtures are deliberately seeded with realistic-looking but
  # fake secrets/PII to test detector accuracy (see benchmark/labels.json).
  # They are not real leaked credentials.
  - "benchmark/corpus/**"
